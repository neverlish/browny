<div class="container">
	<%= simple_form_for([@spot, @ask]) do |f| %>

	  <div class="form-group">
	    <%= f.input :body, input_html: { class: 'form-control' } %>
	  </div>
	  <%= f.button :submit, class: 'btn btn-default'%>
	<% end %>
</div>