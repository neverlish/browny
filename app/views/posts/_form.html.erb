<div class="container">
<%= simple_form_for([@spot, @post]) do |f| %>

  <div class="form-group">
    <%= f.input :body, input_html: { class: 'form-control' }, label: false %>
  </div>
  <%= f.button :submit, class: 'btn btn-default'%>

<% end %>
</div>