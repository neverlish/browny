
CKEDITOR.editorConfig = function (config) {
  // ... other configuration ...

  config.toolbar_mini = [
    ["Image"],
  ];
  config.toolbar = "simple";

  // ... rest of the original config.js  ...
}